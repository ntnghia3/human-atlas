# CODEX TASK — M04B2A Public Research Pack Integration

Start from:
`d85ae6dd54b661c0023c00b8fd470336f14e8c1e`

Owner-provided files:
- `M04B2_NVH2008_PUBLIC_RESEARCH_SEED.jsonl`
- `M04B2_PUBLIC_RESEARCH_PACK.json`

## IMPORTANT

NO WEB.
NO CRAWLING.
NO INDEPENDENT TERMINOLOGY RESEARCH.
NO MEDICAL REVIEW.

This task only integrates the supplied research pack into the existing M04A/M04B1
research pipeline.

The JSONL contains 28 NVH2008 terminology records with source revision
`NVH-2008-312P`. Treat them as RESEARCH EVIDENCE, not production-approved
terminology.

The JSON research pack contains explicit concept bindings and HMU2022
corroboration notes. Those bindings are owner research conclusions and must remain
non-production.

## REQUIRED WORK

1. Validate every JSONL record against:
   `data/terminology/research/bulk-source-corpus.schema.json`

2. Preserve the file under a research corpus location, e.g.:
   `data/terminology/research/corpora/nvh2008-public-research-seed.jsonl`

3. Preserve the research pack under:
   `docs/en-vi/research/` or `data/terminology/research/` as appropriate.

4. Combine this corpus with the existing M04B1 FIPAT TA2 corpus without losing
   provenance.

5. Run the deterministic index and all-3,432 bulk matcher.

6. Add support for the explicit `conceptBindings` only as research annotations if
   the existing M04A output has no field for curated research bindings.
   Do NOT convert an explicit research binding into Atlas↔FMA or Atlas↔TA2 mapping.

7. Preserve the existing M03C conflict/scope decisions:
   - right/left hip bone: conflict remains open;
   - foot navicular: conflict remains open;
   - side-specific structures are not generated by concatenation;
   - singular/plural mismatches such as `external intercostal muscle` vs
     NVH source `External intercostal muscles` remain flagged rather than silently
     normalized;
   - fascia-lata source evidence does not collapse `zone of fascia lata`;
   - generic vertebral-column symphysis does not auto-create level-specific terms.

8. Do not treat English aliases in the source record as Vietnamese aliases.
   Do not activate Vietnamese aliases/search forms from this pack automatically.

## EXPECTED EFFECT

This is an incremental corpus expansion, not M04B2 final completion.

Report:
- TA2 record count unchanged from M04B1;
- NVH2008 research records added: 28;
- 3,432/3,432 concepts processed;
- exact/normalized match deltas;
- conceptBindings applied as research annotations;
- M03C regression status;
- bucket changes;
- production coverage remains zero.

## TESTS

Run existing M03A/M03C/M04A/M04B1 tests plus terminology/localization/check/build,
atlas validation, interaction validation, and `git diff --check`.

Add tests proving:
- source revision mismatch rejects the NVH records;
- an explicit concept binding remains research-only;
- side-specific canonical names are not synthesized;
- conflict concepts remain blocking;
- source plural/singular mismatch is not silently rewritten;
- production overlay/search remains unchanged.

## REPORT

Create:
`docs/en-vi/M04B2A_PUBLIC_RESEARCH_PACK_REPORT.md`

## STOP

STOP after integration.

Do not browse for more sources.
Do not perform medical review.
Do not start bulk translation.

Return:
PASS / PARTIAL / FAIL
final commit SHA
files changed
record counts by source
3432-concept match statistics and delta vs M04B1
M03C regression
production coverage
remaining research gaps
